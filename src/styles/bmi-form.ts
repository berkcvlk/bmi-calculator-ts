import styled from "styled-components";

export const BMIFormHeader = styled.h3`
  margin-top: 0;
  margin-bottom: 35px;

  text-transform: uppercase;
  text-align: center;

  color: var(--primary-color);
`;
